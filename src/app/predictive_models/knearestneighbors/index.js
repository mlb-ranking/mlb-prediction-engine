// import { Vector } from './../datastructures/Vector';

// Dev stuff
import { logger } from 'js-utils';

export class KNearestNeighbors {

  /**
   * Setup a euclid prediction set
   * @param  {Array}  players
   * @param  {Array}  statList
   */
  constructor(results) {
    logger(results);
  }

  nearestNeighbors(player) {
    logger(player);
  }

  
}